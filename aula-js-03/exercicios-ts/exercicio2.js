var cidades = ['São Paulo', 'Guarulhos', 'Rio de Janeiro', 'Belo Horizonte', 'Manaus'];
function listarCidades(cidades) {
    cidades.forEach(function (cidade) {
        console.log(cidade);
    });
}
listarCidades(cidades);
